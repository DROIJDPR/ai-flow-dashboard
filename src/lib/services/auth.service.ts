/**
 * TODO:
 * Integrar backend.
 *
 * POST /api/auth/login
 */
export async function login(
	email: string,
	password: string
) {
	console.log(email, password);

	throw new Error('Backend not implemented');
}